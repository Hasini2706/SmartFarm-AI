# Prototyping & Development Notebooks

This folder is reserved for Jupyter Notebooks used during research and early model prototyping:

- Prototyping features extraction.
- Validating Random Forest parameters.
- Evaluating classification thresholds and regression losses.

*Note: Production training runs via the command-line script in `scripts/train_models.py` to compile models into serialised binary payload formats.*
